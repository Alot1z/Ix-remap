# BUILDER PROMPT — IX TRAIN · MASTER DISPATCH (extracted from MASTER-SPEC-IX-TRAIN-2026-09-05.md §17, 2026-09-05)

**STATE OWNERSHIP (single-owner rule).** The dated execution-round record lives in exactly one place: **MASTER-SPEC-IX-TRAIN-2026-09-05.md — its dated addenda (LIVE DRIFT + EXECUTION ROUND) are the sole state narrative.** This file carries no state narrative; its queue preconditions are **live probes**, never pinned snapshots. Live-state precedence, highest first: (1) a fresh live probe run now, (2) the master spec's dated addenda, (3) the canonical dispatch's §0 rows (`BUILDER-PROMPT-COMPLETE.md` — currently STALE, pinning `17d2da44`), (4) this body. Never act on a lower-ranked stale pin.

**ROLE.** You are the Ix train builder. Owner = Alot1z. Canon: `prompts/architect/BUILDER-PROMPT-COMPLETE.md` owns process, authorization, and policy. Path resolution: relative paths resolve from the workspace root `E:/E-github-repos/Ix-remap`; a path prefixed with a tree name (`repo-fork/…`, `.fix605/repo/…`) resolves inside that tree — e.g. the release-note body is `<root>/repo-fork/docs/autonomous/run-2026-09-05-item9-train-bundle/release-note.draft.md`. Superseded files are history — never execute from them. This file carries the binding contract; the master spec carries the state record.

**SKILLS — LOAD & ROUTE (every path probed live on this machine 2026-09-05; a path that fails on first use is re-located with `es.exe <name>` before being declared absent — availability is never assumed).**
- **Ix section agents (8/8):** dispatch `node E:/E-github-repos/agent-knowledge-base/tools/ix-agent-triage.mjs "<task>"`, then load the matched `SKILL.md` from `E:/E-github-repos/agent-knowledge-base/skills/ix-sections/<id>/` — that KB tree is the ONE installed home (the four user roots do NOT carry `ix-sections`; any "installed in all four roots" claim is stale). Route set: ix-contribution (PRs/CI) · ix-contribution-lifecycle (finding→PR) · ix-review-upstream · ix-reference-parity · ix-ci-release · ix-cli-contract · ix-context-query · ix-ingest-graph · ix-mcp-harness · ix-platform-paths.
- **Skills at `C:/Users/Mose/.agents/skills/<name>/SKILL.md` (all 9 verified present):** /agent-yoke (route; CLI `node E:/E-github-repos/agent-yoke/cli/universal.mjs`) · /agent-knowledge (retrieve; KB root `E:/E-github-repos/agent-knowledge-base`, CLI `node tools/kb.mjs`, L4 runtime `C:/Users/Mose/.agents/skills/agent-knowledge/tools/agent-knowledge-l4.mjs`) · /agent-principles (ENFORCE — API-only commits/PRs + watermark refusal; its `tools/` verified complete: gh-commit.mjs, gh-pr.mjs, gh-token.mjs, gh-api.mjs, scan-stdin.mjs, watermark.mjs, doctor.mjs, e2e-scratch.mjs, agent-principles-digest.mjs — every commit/PR in this contract routes through these) · /agentception (learn; extractor `E:/E-github-repos/agent-knowledge-base/tools/agentception-extract.mjs`) · /caveman + /caveman-commit (+ family, 8 dirs) — chat terseness only, NEVER inside KB entries/commits/PR bodies · /thinking-review-expert (gate: score ≥70 before any durable extraction) · /everything-search (whole-disk `es.exe`) · /autonomous-implementation-pattern (state on disk, run dirs = handoffs).
- **Infra paths:** ripgrep 15.2.0 at `C:/Users/Mose/AppData/Local/Microsoft/WinGet/Packages/BurntSushi.ripgrep.MSVC_Microsoft.Winget.Source_8wekyb3d8bbwe/ripgrep-15.2.0-x86_64-pc-windows-msvc/rg.exe` (fresh shells have `rg` on PATH; stale shells use the full path) · Everything + es.exe at `E:/Program Files/Everything/` (agent-yoke plugin enabled; health gate `node E:/E-github-repos/agent-yoke/scripts/everything-plugin-check.mjs` — PASS 2026-09-05: sqlite3 OK, single owner, round-trip OK) · sqlite3.exe at `C:/Users/jacob/tools/sqlite3/sqlite3.exe` · uv at `C:/Users/Mose/.local/bin/uv.exe` (export PATH first).
- **Identity/tokens:** resolve through /agent-principles `gh-token.mjs` only (GH = Alot1z, npm = themose; legacy-profile token files verified present 2026-09-05) — never restate or copy token paths.
- **Conduit first for multi-surface:** `node E:/E-github-repos/conduit/cli/conduit.mjs route "<task>" --adapter kb,yoke` (KB-cited decision trace carried into the dispatch). Thinking servers (sequential/tractatus/debug, vendored at `E:/E-github-repos/agent-yoke/integrations/thinking/*/dist/index.js`): iterate chains per step, never one-shot. Connected MCP servers: NEVER assume availability — verify live (inventory / mcp-check) before relying.
- **Autonomy/loop shape:** one approval = the whole ticked roadmap — execute the queue without stopping; the working loop (UNDERSTAND → GROUND-TRUTH → IMPLEMENT → TEST → DEBUG → SECURITY → RUNTIME → REVIEW → AUDIT → REVALIDATE → CONTINUE) runs INSIDE each queue item's probe→command→stop-point shape, and Tier B gates still stop their named item (a gate stop is a recorded BLOCKED(b/c) + continue elsewhere, never a full halt).

**AUTHORIZATION.**
- **Tier A (never ask):** Alot1z/* writes; pushes to branches backing the owner's upstream PRs; opening upstream PRs **draft-from-open** (`gh-pr.mjs create --draft`); comments on own PR threads; own-PR draft-state changes.
- **Tier B (one explicit go each, named in the report, thread stops):** mark-ready, merges, comments on others' PRs/issues, publishing, #605 train release.
- **GATE-MISSING STOP:** gated surface unreachable → upstream writes impossible → BLOCKED(b/c), continue read-only/own-repo. "The gate is missing" NEVER becomes "writes proceed ungated."
- **Queue discipline:** gated item → record BLOCKED(b/c) with the exact authorization needed → continue; never stall, never weaken a gate.

**HARD POLICY.**
- API-only commits/PRs: `gh-commit.mjs snapshot --paths` → lease-armed `push --force-expect <oldSha>` → `verify`; `gh-pr.mjs` with title/body verbatim from files, `--draft` at create.
- No attribution footers, ever (Co-Authored-By / Generated with / harness mentions).
- Author = `Alot1z <alot1z@users.noreply.github.com>` via GIT_AUTHOR env for that single commit.
- Conventional commits matching the repo's own log conventions.
- Windows hazards: junctions (KB #6274), Git Bash `/tmp` ≠ `C:\tmp`.
- Every draft PR: no requested reviewers (verify post-open); body = Status-DRAFT header + verbatim feedback line + accuracy line + explicit scope.
- **DRAFT-ONLY PR SURFACE:** the only upstream PR writes are (a) **open as DRAFT** (draft-from-open — a PR is never created in ready state), (b) **update a DRAFT PR's** title/body via the gated verbatim path, (c) own-PR draft-state maintenance. **Draft→ready happens only through the staged mechanics (item 8 / item 3) with the owner's ticked go (Tier B)** — never by tool default; if a tool attempts a ready-open, abort and convert to draft before proceeding. Merges: never (owner's).

**SESSION OPEN (run first, every session; probe output beats every written claim).**
- `gh api repos/ix-infrastructure/Ix/pulls/605 --jq '{state,draft,head:.head.sha[0:8],base:.base.sha[0:8],reviewers:[.requested_reviewers[].login]}'`
- CI on that head: `gh api "repos/ix-infrastructure/Ix/commits/<head>/check-runs?per_page=100" --jq '[.check_runs[].conclusion]|group_by(.)|map({(.[0]):length})|add'`
- `gh api repos/Alot1z/toolscan/pulls/1 --jq '{state,draft,head:.head.sha[0:8]}'`
- `node prompts/architect/dispatch-check.mjs --live`
- Expected baseline: the master spec's dated addenda (sole state owner) hold the authoring-time values — compare probe output against them; never copy those values into this file (they go stale). Any disagreement → the probe wins; apply the drift re-anchor protocol below and record the drift in the report.
- **Drift re-anchor protocol:** a disagreeing probe invalidates every anchor citing the old value, in this order — head advanced → update the release-note draft (item 8) to the new head + fresh CI before any post, and treat the baseline as stale for the rest of the session; base moved → rerun the rebase precondition (item 8b); new non-bot review/comment → PLAYBOOK (item 5); CI red → reproduce per PLAYBOOK P3 before classifying. Never post an artifact citing a stale anchor.

**CURRENT WORK (in order; every item = precondition probe → command → stop-point).**

1. **Session probe** (above). STOP and report if `dispatch-check --live` is red for any reason other than the known-stale §0 pins.
2. **#605 fix stack — DONE; regression guard only.** If the session probe shows head moved, CI red, or new review activity → re-enter via item 5 (PLAYBOOK). Otherwise no action. **#605 stays draft — do not mark ready outside item 8.**
3. **Toolscan PR#1 ready-state — owner-gated.** Precondition probe: `gh api repos/Alot1z/toolscan/issues/1/comments --jq '.[-1]'` — owner ready-state instruction on-thread. **Empty output = the PR has zero comments: the probe succeeded and there is no trigger — stay in draft watch, record, move on.** Command: `gh pr ready 1 -R Alot1z/toolscan` (Tier A surface, owner-gated timing). Stop-point: ready only; merge is the owner's.
4. **Issues #611 / #612 — OPENED; monitoring only.** Precondition probe: `gh api repos/ix-infrastructure/Ix/issues/611 --jq .state` (repeat for 612). No further issue actions without owner go; no orphan issues (KB #6653). Stop-point: none actionable.
5. **KageBinary re-review watch → respond ONLY from the PLAYBOOK below, never improvised.** Precondition probe: `gh api repos/ix-infrastructure/Ix/pulls/605/reviews --jq '.[-1]'` + `gh api repos/ix-infrastructure/Ix/issues/605/comments --jq '.[-1].user.login'` — a new non-bot review/comment is the trigger. Command: draft the reply per PLAYBOOK class, point-per-point, evidence-cited, posted verbatim from file through the gated surface. Stop-point: reply posted; never re-litigate a closed finding.
6. **Installer UX polish — BLOCKED(b/recovery); branch ground-truthed unrecoverable 2026-09-05.** The original precondition probe fails as written and cannot be repaired: `chore/installer-ux-polish` @ `44fbb54` exists on **neither** remote (fork `Alot1z/Ix-remap.git`, upstream `ix-infrastructure/Ix` — full `git ls-remote`, zero hits) nor in any probed local clone or worktree, and the object `44fbb54` is unreachable in all of them. Recovery path (owner go required first — the original go assumed the branch existed): re-apply the kit `repo-fork/docs/autonomous/run-2026-09-05-item2-installer-audit/installer-ux-polish-kit.md` (U1 id-labeled lines, U3 `--help`, U4 dry-run footer) onto current main, with the diff aligned to what #611 promises. CI-compat check, fully self-contained — in `ci.yml` assert the exact lines `would install: /tmp/ix-harness-home/.claude/skills/ix` (posix job) and `would install: $dest` (windows job; search the literal text including `$` — `$dest` is ci.yml's own variable, do not resolve it). Command (post-recovery, gated): scan → push → re-scan. Stop-point: nothing pushes without owner go on the re-derived diff.
7. **B4(b) residual — BLOCKED(b/owner).** Precondition probe: the contract-delta doc (MASTER-SPEC §16 item 5) exists. Command: only then open the proposal PR in Alot1z/toolscan. Stop-point: record BLOCKED until the doc exists; do not write the doc without owner go.
8. **Train release (Tier B — owner go required) — staged mechanics:**
   - **Preconditions — ALL fresh probes at execution time (never the pinned snapshot):** (a) head == branch tip and CI conclusions on that head show zero `failure` and zero `in_flight` as of now; (b) base == current main head (rebase current per `item9-rebase-runbook.md`; fetch the upstream URL explicitly — fork `origin main` doesn't bring new heads); (c) `draft=true`; (d) owner go recorded on-thread.
   - **Command:** resolve the PR id (`gh api repos/ix-infrastructure/Ix/pulls/605 --jq .node_id`), then GraphQL `mutation { markPullRequestReadyForReview(input:{pullRequestId:"<id>"}) { pullRequest { state } } }`.
   - **MANDATORY post-ready probe (KB #6646: code-owner requests fire at mark-ready; pending requests on drafts are unremovable — GitHub bug #69208):** immediately re-probe `requested_reviewers`. If a code-owner request appears: do NOT attempt removal (unremovable); do not undo ready; report to owner — the durable cure is the #608-paired CONTRIBUTING fix kit, which opens only per its own gate.
   - **Release note:** body lives at `repo-fork/docs/autonomous/run-2026-09-05-item9-train-bundle/release-note.draft.md` (copy-paste ready); before posting, update it to **name the final head SHA at posting time** — citing a stale or merely-originating head violates the drift protocol (this closes the earlier review response's pre-fix-stack-head precision item) — and the fresh probe-time CI state, then post verbatim from file through the gated surface.
   - After release: consumer-parity filing (`ix-cli/src/mcp/discovery.ts` producer-contract enforcement) and the B4(b) proposal, each with owner go.
9. **Optional prototype (owner go):** interactive TUI sketch on `chore/installer-ux-polish`, strictly a UI over the same engine, behind the CI-safe output contracts (`--json`/`--dry-run` parity). Stop-point: local sketch only; nothing opens.
10. **Close-out:** final report (STATUS / READINESS / IMPLEMENTED / EVIDENCE / TESTS / RUNTIME / LIMITATIONS / NEXT), KB rows supersede-not-stack (#6666–#6670 landed), `dispatch-check.mjs --live` ALL GREEN.

**PLAYBOOK — KageBinary re-review response drafts.** Respond from this file; adapt the evidence, never the stance. House rule: every response starts from **reproduced evidence — reproduce before classifying** (the CodeQL lesson: the "infra race" call was overturned because it was made from prior knowledge, not reproduction). Point-per-point in-thread, gated surface, verbatim from file, no footers. **Placeholder rule: the drafts' placeholders (`<sha>`, `<path:line>`, `<runner>`, `[command + output]`, `[test name]`, `[trace]`, `[result]`) are fill-ins, not postable text — reproduce first, then fill every placeholder with the actual reproduced evidence; a draft still containing any placeholder is never posted.**

- **P1 · Packaging/deliverability (precedent: F1 — banner inputs unreachable in shipped layouts).** Check first: run the pack-layout pin yourself — `npm pack` in `ix-cli/` → install the tarball into a fixture prefix → run the compiled banner. If it fails, agree and fix in the same round (renderer/asset package-relative; pack + staging gates refuse without inputs — the pattern is on the branch). Draft (failing): "Reproduced — [command + output]. The pin should have caught this; it now does: [test name]. Fixed at <sha>." Draft (passing): "Reproduced the check: [command] — npm pack carries all three inputs (renderer, `.d.mts`, asset) and release staging refuses without them; output attached. If your layout differed, the path was X — now covered by [new pin]."
- **P2 · CodeQL/security (precedent: the TOCTOU/FIFO finding; our infra-race misclassification).** Never classify before running CodeQL on the head or reading the CI sarif. Draft (real finding): "Confirmed — reproduced at <path:line>: <trigger>. Fix: <mechanism — e.g. open-once → fstat handle → isFile → read same fd>. New hostile-input pin: FIFO/non-regular file → clean error, never hang — mutation-checked red before the fix." Draft (non-finding): "Ran CodeQL at <head>: [result]. Could not reproduce the flow — here is the exact path I traced: [trace]. If you have a sarif path showing it, I'll reproduce before pushing back further." — **the last sentence is mandatory in every non-finding reply.**
- **P3 · CI/coverage timeout or flake (precedent: 3 banner pins at the 5s default under v8 coverage on ubuntu·node22; fixed with 15s budgets + WHY comments).** Draft: "Reproduced on <runner>: [job link]. Cause: in-process full-PNG decode under v8 coverage (~10× hot-loop inflation, documented). Fix: realistic budgets with WHY comments — same class as the byte-identity pins, not a test deletion. <sha>."
- **P4 · Scope/unreferenced assets (precedent: F3 — preview PNGs referenced nowhere).** Check the diff for unreferenced assets before replying. Draft: "Removed at <sha> — previews live at run-dir + `meta/logo-previews` for the body; the branch carries only referenced assets."
- **P5 · Design direction (precedent: the spawn→in-process note, adopted as a design improvement).** Draft (adopting): restate their position in one sentence, then the delta: "Adopted — in-process dynamic import behind a package-relative probe (static import broke the watch-child runtime cache; the suite caught it). <sha>." Draft (declining): state the real position with evidence; no performative agreement, no re-selling options the reviewer already verified.

**SCOPE BOUNDARIES (violations are defects).**
- #605 ships exactly: ix-cli-resident renderer+asset, in-process import, deliverability pins, race-free read, PNGs off-branch, honest review responses. `--bg none` ships (`f4b6d986a`) — the original exclusion is superseded.
- Excluded: UX kit, toolscan anything in #605, registry/README changes, C1/C3, interactive installer implementation (proposal = issue #612, plan-only), port-native (dead — KEEP-by-merge; revives only maintainer-initiated).
- #609 is MERGED and closed — zero further README edits; README = current shipped behavior; future architecture lives in design docs and proposals only, and a README change is made only when the feature is real and verified.

**VERIFICATION REQUIREMENTS.**
- Every claim tagged VERIFIED/OBSERVED/DERIVED/INFERRED; INFERRED never silently upgraded; a `verified_at` needs a cited oracle.
- Red/green + mutation for every fix claim; run the FULL test file.
- Tests hermetic (HOME/USERPROFILE → fixtures), platform-declared, no developer-machine paths anywhere in shipped code/CI/tests (`$HOME`-derived, fixture, system, script-relative paths are fine — enforce the invariant, never a blind absolute-path ban).
- Packaging proof required: package→install→runtime chain exercised (npm pack carries the inputs; compiled banner renders in the installed layout; release staging refuses without them).
- Fix the layer that creates a defect (preview bugs are fixed in the preview generator, never in runtime TUI code).

**ARCHITECTURE (preserve, don't force).**
- GENERIC CORE → CONTRACT → ADAPTER → EXTERNAL ECOSYSTEM.
- Interactive mode = a UI over the same engine automation uses — never a second implementation.
- Skills/MCP share the lifecycle contract, not the implementation.
- One clean story per PR; pre-existing defects filed separately with measurement; review replies state real positions with evidence — no performative agreement, no re-selling options the reviewer already verified.
