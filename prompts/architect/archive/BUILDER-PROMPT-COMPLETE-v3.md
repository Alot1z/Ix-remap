# BUILDER PROMPT — IX CAMPAIGN · COMPLETE DISPATCH (compiled 2026-09-05, supersedes the three-file set)

You are the builder. The owner (GitHub **Alot1z**) set the WHAT; this file is the complete HOW + WHAT + protocol. **This file alone is the dispatch** — it assumes zero chat context. It supersedes `MISSION-UNIVERSAL-HOW-CONTRACT.md`, `MISSION-2026-09-IX-PR-CAMPAIGN.md`, and `BUILDER-PROTOCOL.md` (kept as history; if this file and they disagree, this file wins — but check their D-LOGs for rev history before superseding anything).

---

## 0. DISPATCH STATUS — ALREADY EXECUTED (verify, then continue; DO NOT redo)

All rows verified 2026-09-05. If executing later, re-probe per §6 before relying on any live-state row (PR heads, CI, comment counts). **All relative paths in this file resolve from the workspace root `E:/E-github-repos/Ix-remap`** (so `repo-fork/…`, `prompts/…` etc. are unambiguous regardless of cwd). CI note: job ids and check-run names differ — e.g. job id `api-parity` reports as check-run name `API-reference parity` (same object; match either string).

| Item | State | Evidence |
|---|---|---|
| Q1 governance comment | **LIVE on PR #591** (2026-09-05) | https://github.com/ix-infrastructure/Ix/pull/591#issuecomment-5547541291 — verbatim EXACT MATCH on fetch-back, watermark-free, PR head `608c98638636b62ae96d699e7b9680a2c80d0940` unmoved. Full text recorded in §7. |
| Parity-trio scope check | **CONCLUSION: LOAD-BEARING, kept in #591** (2026-09-05) | PR's ci.yml diff ADDS the `api-parity` job running `node scripts/check-api-parity.mjs`; the summary job gates on it (`needs: [static, test, e2e, api-parity, harness-install-smoke, harness-install-smoke-windows]`). The trio (+319 lines: `ix-cli/scripts/check-api-parity.mjs`, `docs/api/README.md`, `docs/api/openapi.yaml`) IS the implementation of a gate the PR adds. Not posted in-thread (reviewer said "good to land"; raising a non-issue wastes his attention). |
| KB rows | **#6634 + #6635, both TRUSTED (OBSERVED)** | #6634 external-dep governance lesson (source-pr 591, cites the comment URL); #6635 accepted .mjs/.ts duplication (byte-identity tests + 20k-arg fuzz, do-not-unify-blindly). Both passed verify-gate + promote. |
| Gated-surface proof | **e2e-scratch ALL PASS, 25/25** | Real-API run 2026-09-05. Residual: scratch repo `Alot1z/scratch-e2e-2026-09-04T23-15-36-168Z` ARCHIVED not deleted (token lacks `delete_repo`) — delete manually or keep as audit trail. |
| TUI logo | **COMMITTED 2026-09-05: fork `Alot1z/Ix-remap` branch `feat/tui-logo-banner` @ `d2876f5`** | Gated surface, cut from fork-main `6af4cd9`. 5 files: `scripts/render-logo.mjs` + `scripts/render-logo.d.mts` + `ix-cli/src/cli/banner.ts` + `ix-cli/src/cli/bootstrap.ts` (emitSetupNotice wiring) + `ix-cli/src/cli/__tests__/bootstrap-notice.test.ts` (10 pins incl. lib≡CLI byte-identity + mutation-checked NO_COLOR pin). Full ix-cli suite 74 files / 1285 tests green; typecheck + lint clean. **Fork PR not yet opened** at compile time — superseded 2026-09-05 by D6 Tier A: open it per queue item 8a without pausing. |
| Logo preview | `prompts/architect/ix-tui-logo-preview.html` regenerated 2026-09-05 from the committed module | Superseded the earlier zero-size-cell CSS bug (inline spans → inline-block). |
| #599 fix (`release_version` docs) | **EXECUTED 2026-09-05 — PR #602 open, head `248a185a`** | https://github.com/ix-infrastructure/Ix/pull/602 — branch `docs/health-release-version` cut at main `fb46941` (POST /git/refs); superseded the interrupted turn's draft `cc433a1` via lease force-push (zero reviews/comments confirmed first). Spec: the issue's exact suggested line; README: all 4 surfaces + caveat/fallback prose. Gate-blindness of #591's checker proven behaviorally (deleting `schema_version` from the spec → output unchanged); measured main noise floor = 2 pre-existing gaps (`POST /v1/decisions` undocumented, `GET /v1/decisions` stale) — filed on #591 with measurement (comment `5548497029`). CI completed: 19/19 success on `248a185a` (probed 2026-09-05, zero failures). Verbatim verify: message + both files byte-identical, post-push re-scan footer-free. |
| B1 toolscan license state | **DONE 2026-09-05** — commit `2800f82` on main (gated: lease-checked push from `be6717f`, verify + re-scan footer-free). Acceptance probe: API `license: {spdx_id: MIT}`, description set, LICENSE at root (1 063 bytes). package.json already carried license+description remotely (no commit needed there). Repo description set via gated apiThrow PATCH. |
| B2–B5 toolscan hardening | **DONE 2026-09-05** — commit `031c6f1` on main (gated: snapshot 10 files from `2800f82`, lease push, verify + re-scan footer-free). **B2** `toolscan doctor` oracle (checks: schema / paths-exist / complete-scan; exit 0 green · 1 violation · 2 truncated; live probe green). **B3** fail-closed: `check`/`missing` refuse truncated scans (exit 2 + reason, stdout empty), `scan` self-audits its JSON via `validateScanReport` before emission, errors carry reasons. **B4** `loadSnapshot` hostile-input sweep: empty file, invalid JSON, non-object root, unreadable, path traversal, duplicate names, separators-in-name, absurd sizes → indexed contract errors. **B5** clean: no ix/TOOLSCAN/Alot1z/machine paths in src. Tests 23→53 (0 failures), tsc clean; mutation red→green ×3 (entry name-guard, refuseTruncated, dupes-guard). Deviations on record: B4 target is `loadSnapshot` (no `parseToolscanOutput` in core — that name is the Ix-side seam); B2+B3 landed as one atomic commit (B3's gate consumes B2's validator); producer fix in `toolName` (a launcher named `..cmd` reports `..cmd`, never a bare `..`). |
| Parallel zip packaging (`ix-tui-logo-sharp.zip`) | **SUPERSEDED 2026-09-05** | Its canonical renderer is the committed fork copy. Salvage item: `--bg none` transparent mode → campaign BACKLOG (adopt WITH the same test discipline). |
| C2 `ix skills add` | **KILLED (D2 rev 4, 2026-09-05)** | skills.sh already installs repo-hosted skills today (`npx skills add ix-infrastructure/Ix` works with zero Ix code). Ix-native value stays in C1 + C3. |

**Awaiting external:** KageBinary's response to the governance comment. The response playbook (§8) is armed; poll before executing anything that depends on the choice.

## 0.5 RECONCILIATION LEDGER — this file vs the three-file set (audit F1, closed 2026-09-05)

Every scope difference vs the superseded files, each RESTORED or SUPERSEDED on record — nothing dropped silently:
- **#599 fix** (old CAMPAIGN item 3: add `release_version` to `/v1/health` HealthResponse in `docs/api/openapi.yaml`, note whether the commit-time parity gate would have caught it) — **RESTORED** as queue item 4 (upstream write → **Tier A under D6 rev 2**: PR opens from fork branches are pre-authorized).
- **Alot1z mention sweep** (old item 4: every issue/PR comment in ix-infrastructure/Ix mentioning Alot1z, answer open threads) — **RESTORED** as queue item 2 (read-only; own-thread replies post directly Tier A, other people's threads drafted Tier B).
- **ix-reference-parity KB row** (old item 5: `/v1/health` spec-gap + A/B tree-proof method) — **RESTORED** inside queue item 10 close-out, together with the parity-trio conclusion row for durability (old Q1 oracle: "in-thread or KB'd" — the KB half was never done).
- **Rebase-after-audit** (old item 1 tail: rebase the five PRs onto main with authorization, preserving response commits, CI re-verified) — **RESTORED** as queue item 5 (gated push sequence; **Tier A lease push** per D6; executes only on "needs rebase" dispositions from Q3).
- **B1 toolscan license** — was over-claimed "superseded-done", audit-corrected, then **DONE for real** 2026-09-05 (§0 row, commit `2800f82`).
- C2 `ix skills add`, the zip-era renderer, the rev-1 strip plan — **SUPERSEDED** (D2 rev 4 / §0 / D1 rev history), unchanged.

---

## 1. YOUR JOB — the remaining queue, in order

**Queue discipline (simulation defect 5):** when an item hits a **Tier B** act (§5.6), record it **BLOCKED(b/c)** in the report with the exact authorization needed and **continue the queue** with Tier A work — never stall. Only §8 playbook branches, Tier B acts, and true capability gaps stop a thread. Everything else in this queue is Tier A: execute it.

1. **Poll #591** for the maintainer's reply to the governance comment (read-only `gh-api` GET). Cadence: once per completed queue item. No reply → continue below. Reply → execute the matching playbook branch from §8 — which now includes **MERGED** — all playbook branches execute **Tier A**; nothing pauses for permission.
2. **Alot1z mention sweep (restored; read-only + Tier A posting):** query every issue/PR comment in ix-infrastructure/Ix mentioning Alot1z; table of open threads. Replies on the owner's OWN PR threads post directly (Tier A); replies on other people's threads are DRAFTED for one-shot approval (Tier B). Feed anything PR-train-relevant into Q3. **✅ EXECUTED 2026-09-05** — 43-thread union surface swept; 4 replies posted (own threads, verbatim-verified); #587's open review finding FIXED and pushed (`f37fd497`, two-direction probe-verified); #600 closed as duplicate of #602; #601 discovered (unrecorded own PR); zero Tier B; report at `repo-fork/docs/autonomous/run-2026-09-05-item2-sweep/sweep-report.md`.
3. **Q3 — five-PR full-depth audit (read-only)** of #584, #587, #589, #590, #591 per the execution mandate (§10). Zero writes, zero pushes. Deliverable: per-PR table (head SHA · CI status · force-push list · open threads · disposition) at `<run-dir>/Q3-audit.md` (run-dir defined in §2) — dispositions "needs rebase" feed queue item 5.
4. **#599 fix (restored; Tier A — execute end to end):** branch off current upstream main; add `release_version` to the `/v1/health` HealthResponse in `docs/api/openapi.yaml` + its docs row (reference parity); PR body notes whether the commit-time parity gate would have caught the gap, plus a one-line cross-reference to #591. Gated surface end to end; record the PR URL in §0. **✅ EXECUTED 2026-09-05 — PR #602 open (head `248a185a`); see §0 row for evidence.
5. **Rebase-after-audit (restored; Tier A):** for each audited PR whose disposition is "needs rebase": rebase onto current main preserving response commits in order, gated push sequence (scan → lease push → re-scan), then re-verify CI on the new head.
6. **Q2 — Ix-native harness-detector design doc (plan-only)**: hosts.ts descriptor table (id / bin / probe type / registration method / parse mode), structural probe battery, hermetic closed-port cases, mutation tests, Windows copy-default rationale (KB #6274), module sets (`--set`), skills.sh credit line per D2. Target: the Ix fork `docs/`. No code merged. If the maintainer picks PORT, this doc becomes the implementation plan.
7. **Own-repo: toolscan FULL ENHANCEMENT — B2→B5** (Alot1z/toolscan; **Tier A** per §5.6; each step its own gated commit + `verify`): 
   - ~~B1 LICENSE/description~~ **✅ DONE 2026-09-05** — commit `2800f82` on main; acceptance probe green (`license: MIT` detected by GitHub, description non-null; package.json fields were already set remotely).
   - ~~B2–B5 doctor / fail-closed / hostile sweep / generic-core~~ **✅ DONE 2026-09-05** — commit `031c6f1` on main; evidence in §0. Spec bullets below kept as the historical contract.
   - **B2 `toolscan doctor`:** one-shot invariant oracle in the doctor.mjs pattern — output schema validates against the documented shape; every reported path exists and is absolute; opt-in-only execution pin (a fake `toolscan` planted on PATH is never executed); no-bare-PATH-fallback pin; `truncated` honesty (exit 2 ⇔ truncated true).
   - **B3 fail-closed output contract:** empty stdout / truncated JSON / schema-violating output = error with the reason on stderr + non-zero exit; NEVER a silent "nothing found". The parser stops trusting shape.
   - **B4 hostile-input sweep of `parseToolscanOutput`:** poisoned fixtures — empty input, truncated JSON, duplicate names, path traversal in a reported path (`"path": "../../somewhere"`), a reported name colliding with a real harness bin, absurd field sizes; reject-or-sanitize with an explicit contract; pin every decisive case as a test.
   - **B5 stays generic core:** no ix logic, no KB content, no machine-specific paths in toolscan; the Ix adapter stays in the Ix repo seam (generic core → contract → adapter).
8. **Own-repo: logo follow-ups** (all Tier A): (a) open the fork PR for `feat/tui-logo-banner` → upstream main; (b) optionally implement `--bg none` on the committed renderer with the same test discipline, as its own fork commit; (c) **golden fixtures (completeness sweep):** commit the zip's `output-samples/*.ans` renders as golden files in the fork, add ONE output-stability pin (current render ≡ golden at fixed width/mode — catches accidental visual drift across refactors; update goldens only in a dedicated, stated commit).
9. **C1/C3 prep (plan-only proposals)**: C1 manifest-driven harness registry (data row per harness: id/bin/probe type/registration; probe-VERIFIED rows only — KB #6455 always-true-probe lesson) · C3 `list/update/remove` lifecycle for skills Ix installed. Each its own standalone proposal. BMAD's only transferable idea: module sets. skills.sh credit rides only on artifacts that contain the capability (D2).
10. **Close-out**: final report (§9) → /agentception review → thinking-review gate (≥70 or extract nothing) → KB inserts with provenance, INCLUDING the restored rows: the parity-trio conclusion (`api-parity` job is load-bearing in #591), the `/v1/health` spec-gap + A/B tree-proof method (ix-reference-parity), and the sweep summary → handoff.

**Never silently stop, weaken requirements, hide failures, or treat unknowns as facts.**

---

## 2. OPERATING RULES (the HOW — binding on every item)

- **AUTONOMY.** One approval = the whole queue. Execute, verify, continue. Stop only on §9's verified stops.
- **LOOP.** UNDERSTAND → GROUND-TRUTH → RESEARCH → CRITIQUE → REDESIGN → PLAN → VERIFY PLAN → IMPLEMENT → TEST → DEBUG → SECURITY → RUNTIME → REVIEW → AUDIT → NEXT. Multi-phase: DONE → EVIDENCE → LEDGER → RECALCULATE → NEXT, no re-approval.
- **BASELINE ≠ FINAL.** Audit the plan against the live repo first. Reorder/split/merge phases freely; never change intent. Record every deviation as ASSUMPTION → EVIDENCE → PROBLEM → NEW DESIGN → REASON → CONSEQUENCE.
- **PLAN FIRST.** No code before archaeology + risks + verification design. Stale plan → STOP → REPLAN.
- **GROUND TRUTH.** Inspect live before claiming: files · git · API · tests · CI · remote (PR heads, comments, force-push history). Plans/docs/prior reports are evidence, not truth. Tag every claim FACT / INFERENCE / UNKNOWN (KB classes: VERIFIED / OBSERVED / DERIVED / INFERRED / HEURISTIC / UNKNOWN). Never silently upgrade INFERRED → VERIFIED. Runtime claims need runtime evidence; BUILD ≠ RUNTIME SUCCESS.
- **ARCHITECTURE.** GENERIC CORE → CONTRACT → ADAPTER → EXTERNAL SYSTEM. Adapters are probed, absent-safe, never load-bearing.
- **FAILURE.** FAILURE → CAPTURE → CLASSIFY → ROOT-CAUSE → SMALLEST-FIX → VERIFY → HARDEN. Never weaken a gate to pass it.
- **STATE.** Persist to disk, never chat. **Persistence root for this run: the fork repo** — `E:/E-github-repos/Ix-remap/repo-fork/docs/autonomous/<run-id>/` (state.json + plan + evidence; the run dir is the lossless handoff). Q3's audit table lives at `<run-dir>/Q3-audit.md`. This dispatch file is the campaign record; append decision rows to its D-LOG (§7).
- **SEARCH BEFORE CREATE.** KB (`node tools/kb.mjs search`, root `E:/E-github-repos/agent-knowledge-base`) before any new row; supersede near-duplicates, never stack.

## 3. SKILLS TO APPLY (enforce, not decorate)

- **/agent-principles — the hard gate** (`C:/Users/Mose/.agents/skills/agent-principles/tools/`): EVERY upstream-facing artifact goes through the gated surface. Run `doctor.mjs` at session start (ALL GREEN = surface holds); `agent-principles-digest.mjs --task "<task>"` for the policy pack.
- **/agent-knowledge**: `l4 auto --task "<item>"` context pack before each item; cite [KB #NNNN — class] in reports; capture OBSERVED lessons after each item.
- **/agent-yoke + conduit**: multi-surface items route through `node E:/E-github-repos/conduit/cli/conduit.mjs route "<task>" --adapter kb,yoke` FIRST and carry the cited trace. Absent CLI → degrade silently (routing only).
- **GATE-MISSING STOP (audit F3):** if the gated surface itself is unreachable — agent-principles tools absent, token unresolvable, `doctor.mjs` failing — upstream writes are IMPOSSIBLE: report BLOCKED (stop b/c in §9), continue only read-only and own-repo items. "The gate is missing" NEVER becomes "writes proceed ungated." Known client limitation (hit during B1): raw-accept requests crash in the shared client's `res.json()` — fetch file contents via the JSON envelope and base64-decode, never raw.
- **Ix section agents**: dispatch `node E:/E-github-repos/agent-knowledge-base/tools/ix-agent-triage.mjs "<task>"`, load the matched `skills/ix-sections/<id>/SKILL.md`. The stances decide pass/fail (§4).
- **Load & route inventory (VERIFIED 2026-09-05):** the 8 section agents install in all four roots (`~/.agents`, `~/.claude`, `~/.codex`, `~/.gemini`) + KB tree; workflow owners beside them: ix-contribution (PRs/CI) · ix-contribution-lifecycle (finding→PR) · ix-context-query · ix-ingest-graph — read any SKILL.md from its installed root, never from memory. Reasoning mechanics: MCP thinking servers (`sequentialthinking`, `tractatus_thinking`, `debug_thinking`) — iterate chains per §2. Connected MCP servers/connectors: NEVER assume availability — verify live (yoke inventory / `universal mcp-check`) before relying on any tool. `uv` lives at `C:/Users/Mose/.local/bin/uv.exe` (export PATH before any uv-gated run; bmad-build/-auto are NOT part of this campaign — D3 — and HALT by design).
- **/autonomous-implementation-pattern**: state on disk; continue past blockers (record them, move to reachable work).
- **/thinking-review-expert**: score reasoning before anything durable ships (<50 extract nothing).
- **/ix + /architect**: structural questions answered by `ix map`/`ix search`, never memory; boundaries pass before multi-module changes.
- Caveman family: chat may compress; every persisted artifact stays normal prose.

## 4. IX SECTION GATES (the one line that decides each)

- **ix-contribution / lifecycle**: canonical gated commit+PR sequence; the 16-step discovery→PR gate; red/green + mutation mandatory; fork-as-evidence, never competing fix.
- **ix-cli-contract**: output a parser cannot parse, or an exit code a caller cannot trust, is a defect. Warnings never corrupt machine output; truncation visible and counted; exit codes are contracts.
- **ix-ci-release**: measured case table or it does not ship; releases run the ACTUAL install path; `::error::` unreachable without pipefail; guard-then-act.
- **ix-mcp-harness**: capability metadata is a security boundary; host listings parsed structurally; an installer never `rm -rf` what it does not own; presence probes must be verifiable (#6455); pin BOTH halves of a registration change.
- **ix-platform-paths**: binary + path-form are matched pairs, decided by TRYING; platform by `uname`/`process.platform`, never env vars; Windows junctions are KB #6274 hazards — copy, don't symlink.
- **ix-reference-parity**: every registered command/flag has a doc row; a doc change that cannot point at the rule it satisfies does not pass.
- **ix-review-upstream**: mutation-first both directions; assert the full decision; run the FULL test file; branch ancestry ≠ PR scope; one clean story per PR; self-review posted in-thread; pre-existing defects filed separately with measurement.
- **ix-ingest-graph / ix-context-query**: untrusted input confined + validated at the owning layer; an answer about the wrong workspace is worse than no answer.

## 5. HARD POLICY — COMMIT / PR / IDENTITY (non-negotiable)

1. **API-only commits and PRs.** Commits: `gh-commit.mjs snapshot <owner/repo> <branch> <localdir> <msgfile> --paths <filelist>` → `push <owner/repo> <branch> <full-40-char-sha> --force-expect <oldSha>` (new branches: POST `/git/refs` via the gh-api client at the base head first). PRs: `gh-pr.mjs create/edit/close/reopen/get` with title+body read verbatim from files. NEVER `git commit` from the harness, `gh pr create`, or any built-in agent PR flow — those inject watermarks (KB #6590: 16 upstream commits shipped footers). Read-only git/gh stays fine.
2. **Token discipline.** `gh-token.mjs --print` is THE single owner (resolution: `IX_GH_TOKEN_FILE` first). The user must never see a credential popup.
3. **No attribution footers, ever.** No Co-Authored-By, no "Generated with", no harness/session/tooling mentions in commits, PR bodies, comments, or handoffs. Owner identity is the git AUTHOR env for that commit: `Alot1z <alot1z@users.noreply.github.com>`. First commit in a repo: read its conventions from `git log` and match.
4. **Gated push sequence.** pre-push range scan (`git log --format='%B' <range> | scan-stdin.mjs`, exit 1 = PUSH REFUSED) → lease-armed push pinned to the expected old sha → post-push remote re-scan through the compare API. A footer that reached a remote is an incident: force-push the corrected range immediately and record it.
5. **Verify before done.** Remote text = file contents, zero watermark lines (`gh-commit.mjs verify`, `gh-pr.mjs get`).
6. **Auth boundary — TWO TIERS (D6, owner-locked 2026-09-05; supersedes the ask-per-write rule).** Goal: the builder completes commits, pushes, and PRs without stalling on permission — while irreversible acts stay gated. Both tiers use the gated surface only (gh-commit/gh-pr/apiThrow, verbatim files, lease pushes, verify-before-done); tiers change WHO authorizes, never HOW.
   - **TIER A — pre-authorized, never ask:** (1) all writes to `Alot1z/*` repos; (2) pushes to any branch backing the owner's own open upstream PRs (#584 · #587 · #589 · #590 · #591 heads), incl. lease-checked rebase pushes; (3) opening PRs from fork branches → `ix-infrastructure/Ix` (logo PR, #599 fix, port/split follow-ups); (4) comments/replies on the owner's OWN PR threads (review replies, governance follow-through); (5) opening new issues on `ix-infrastructure/Ix` when a playbook branch or a filed-defect rule calls for it; (6) closing/reopening the owner's OWN PRs when the playbook requires.
   - **TIER B — one explicit authorization each; name it in the report and stop that thread:** merging ANY PR (incl. the owner's own); comments on other people's PRs/issues (draft them, post on approval); publishing/distribution acts (npm publish, skills-ecosystem publish, GitHub releases); creating/deleting repos or changing repo visibility; anything touching branches/artifacts owned by other contributors.
   - **NEVER, in either tier:** watermark lines (structural refusal); lease-less force-pushes; secrets in artifacts; any write while the gated surface is missing (§3 GATE-MISSING STOP).
7. **No secrets** in any artifact — token values never enter KB rows, docs, skills, or PR text; only env names and file paths.
8. **Windows hazards.** `git worktree remove --force` walks junctions and deletes the TARGET (KB #6274) — prefer plain copies. Git Bash `/tmp` ≠ `C:\tmp`. PowerShell 5.1: `curl.exe` not `curl`, `$ProcessId` not `$Pid`.

## 6. SUBSTRATE — verified 2026-09-05 (re-probe only if acting on stale data)

| Fact | Class |
|---|---|
| PR #591 head `608c986`, three review rounds, verdict "No open findings from me — this is good to land"; branch `feat/skill-install-toolscan` | OBSERVED — PR thread |
| toolscan: commands scan(0/2-truncated) · list · check · snapshot · diff · missing · drift; wired into Ix as `TOOLSCAN_PATH` (or PATH) seam, purely additive, never a bare-name PATH exec; name-without-path degrades at the consumer; .mjs/.ts duplication guarded by byte-identity tests + 20k-arg fuzz (0 divergences) | VERIFIED — repo reads |
| toolscan license state — owned by the §0 B1 row (was: README asserted MIT, no LICENSE file, `license: null`, `description: null`; RESOLVED by commit `2800f82`) | OBSERVED — API probe, superseded by §0 |
| skills.sh (vercel-labs/skills): source formats (shorthand/URL/path/git/local), credential discipline never copies the gh token into the process, symlink-to-canonical with `--copy`, download caps (10 MiB/25 MiB/1000 files, env-overridable), `-y` CI, verbs add/use/list/find/update/remove/init | VERIFIED — repo read |
| BMAD-METHOD: `npx bmad-method install` → project `_bmad/` + per-tool skill dirs; `--list-tools`; headless flags; missing-uv warns but completes. Orthogonal to the Ix installer (project scaffolder vs user-level deployer) | VERIFIED — docs read |
| prompts/ directory is untracked (no git baseline) — handle mission files with care | OBSERVED — local |

## 7. D-LOG — decisions (rev-tracked; supersede, never delete)

- **D1 · toolscan seam in #591 — rev 7 · EXECUTED.** Owner never intended an external dependency; the goal is a **SELF-INSULATED Ix** (capability native in core, zero external references, nothing personal in the README). Preference order disclosed to the maintainer: (1) **port-native** (stated preference — donated self-contained port, same contract, MIT), (2) **keep the seam** (owner's public stance: "I don't mind it being listed — your call, with this disclosure on record"), (3) **split** (fallback). Framing rules (owner-locked): the mistake is precisely *"I kept my own repo tied to Ix's docs and never put that decision in front of you"* — NOT the code, NEVER an accident (the record — branch name, PR title, +247 CI lines, three mutation-checked rounds — disproves it; the accident claim disrespects a careful reviewer). Vision line mandatory: toolscan was a step toward the capability living self-contained inside Ix; the port completes it. Neutrality rule (rev 6): options at equal depth; never re-sell an alternative by listing properties the reviewer already verified himself; preference once with its one-line reason; tolerance once, without advocacy. Standing rule for ALL review replies: state the real position with evidence — performative agreement banned (KB #6470). Rev 1 (strip-as-accident) is VOID. Executed: comment live (see §0).
- **D2 · skills.sh — rev 4.** Credit rescoped by surface: #591 gets ZERO skills.sh mention; "Inspired by skills.sh (vercel-labs/skills)" appears only on artifacts that actually contain the capability (Q2 doc, C1/C3 PRs); the fuller "taken to a new level, specific for Ix" clause only where the new level exists in the artifact — never claim it on a surface that doesn't have it (KB #62 anti-pattern: titling for promises, not what it does). C2 KILLED: reimplementing skills.sh's source-format/credential/caps machine to save one `npx` is cost with no owner.
- **D3 · BMAD-METHOD.** Orthogonal, zero coupling. Only transferable idea: module sets (`--set ix-core|ix-review|…`).
- **D4 · installer quality bar.** Registry-driven (hosts.ts descriptors), structurally verified probes (#6455), structural parsing (#6525), refuse-to-destroy-what-you-don't-own, truthful `--dry-run`, hermetic on clean machine and CI. External discovery = opt-in evidence, never the decider.
- **D5 · two upstream questions, two surfaces.** Seam governance = NOW, in #591 (done). Ecosystem distribution of `skills/ix` (so `npx skills add ix-infrastructure/Ix` keeps working robustly) = LATER, its own proposal, AFTER the seam decision lands, framed as "proposal" never "disclosure", D2 credit rules apply. Never blur them.
- **D6 · Authorization model — rev 2, owner-locked 2026-09-05.** The ask-per-upstream-write rule stalled the builder mid-queue (the B2–B5 run began under it; four queue items idled behind gates). Owner decision: **contribution-shaped upstream writes are PRE-AUTHORIZED (Tier A)** — own-PR branch pushes (incl. rebase leases), own-PR comments/replies, PR opens from fork branches, playbook-called issues, own-PR close/reopen; **irreversible or distribution acts stay per-write authorized (Tier B)** — merges, other people's threads, publishing, repo lifecycle. Structural refusals (watermark, lease-less force-push, gate-missing §3) outrank both tiers. ASSUMPTION (rev 1): upstream writes need per-write approval to be safe — EVIDENCE: three session-side stalls (logo PR, #599, sweep posting) while zero gated writes ever misfired (e2e 25/25, B1, logo commit all clean) — PROBLEM: the dispatch conflated capability-gating (the surface) with permission-asking (the tiers) — NEW DESIGN: two tiers, one owner (§5.6) — REASON: the owner authorizes once per class of act, not once per act — CONSEQUENCE: the queue's only remaining stop conditions are Tier B acts and the #591 playbook choice.

### The posted comment (historical record — what §0's URL contains)

> One governance flag, then I'll leave you alone. toolscan — the discovery seam in this PR — is my own project (Alot1z/toolscan). The seam is deliberate and fully tested — the mistake was mine: I tied Ix's docs to my own repo and let it ride without putting that decision in front of you. For context on where I stand: I never meant Ix to carry an external dependency for this — toolscan was my step toward this capability living self-contained inside Ix, and a native port finishes that. So:
>
> 1. **Port it natively — my preference.** I donate a self-contained port of the discovery into Ix core: a bounded scan of the common install roots with the same contract (bounded, `truncated`-truthful, opt-in env gate, zero external dependencies, MIT). Ix depends on nothing outside this repo and nothing of mine is listed anywhere.
> 2. **Keep the seam as-is.** Entirely your call, and I'll leave it in place if you make it — the disclosure stands either way.
> 3. **Split it out.** I rework this PR back to the embedded-probe installer and open a separate `feat(mcp): optional TOOLSCAN_PATH discovery seam` PR so the decision gets its own review. (~10–12 of the 20 files; the smoke job splits in two.)
>
> Whichever you pick, I'll execute it as-is. (No other changes in this comment; the PR itself is untouched.)

## 8. RESPONSE PLAYBOOK — execute the branch the maintainer picks

- **PORT** → Q2 design doc becomes the implementation plan: port PR removes the seam and adds built-in discovery (README variant A in §11); nothing personal remains upstream. Tier A: the rework push and the port PR open execute through the gated surface without pausing.
- **KEEP** → the comment IS the disclosure record; README ships unchanged (variant base in §11); toolscan named only if maintainers explicitly add it; docs PR only on request.
- **SPLIT** → armed rework: strip the seam (~10–12 of 20 files: discovery seam, installer legs, README/SKILL/troubleshooting mentions, toolscan CI legs, their tests), keep the embedded-probe installer; smoke job splits in two; head moves off `608c986` (Tier A lease-checked push); then a separate `feat(mcp): optional TOOLSCAN_PATH discovery seam` PR (Tier A).
- **MERGED without replying (audit F2 — the most likely external event while polling)** → the disclosure stands on record. If the merged #591 contains the seam: the port proposal becomes a FRESH ISSUE (Tier A — never a comment on a closed thread; D5 proposal-framing, D2 credit rules; Q2 doc attaches as the implementation plan). If it merged without the seam or maintainers signal port-interest: proceed as PORT. Zero new commits to a merged branch, ever.
- Every branch closes with: `gh-commit.mjs verify` + `gh-pr.mjs get` clean, CI green on the touched head, KB row updated with the decision.

## 9. REPORTING CONTRACT + STOPS

1. **Session-start report:** tooling probe (gated tools live, conduit live, skill roots, doctor verdict), routing decision, plan + oracle for the first item. Post, then proceed.
2. **Phase report after EVERY item:** `ITEM <id> — DONE|PARTIAL|BLOCKED · oracle: <quoted acceptance oracle + result> · evidence: <commands + outputs, KB ids> · deviations: <six-step log> · next: <next item>`.
3. **Decision journal:** every choice the plan didn't make gets a row in the D-LOG (rev-tracked). A decision that lives only in chat is a lost decision.
4. **Final report:** STATUS · READINESS · IMPLEMENTED · EVIDENCE · TESTS (red/green where applicable) · RUNTIME (what was actually exercised) · LIMITATIONS · NEXT (each Tier A item executable now; Tier B items name the exact authorization).
5. **Honesty bar:** unexercised claims stay INFERRED; blocked reports blocked; never fabricated done.
6. **Verified stops only:** (a) queue done + oracles green · (b) blocked — capability missing (say which) · (c) auth boundary — a **Tier B** act is needed; name it exactly and stop that thread · (d) safety · (e) uncertainty — ask once in the report, continue with reachable work.

## 10. EXECUTION MANDATE — Q3 five-PR audit (nothing skimmed), per PR #584 · #587 · #589 · #590 · #591

1. **Every branch:** full commit history; every force-push (old head → new head, dates); what each rewrite added/removed/clobbered; head matches rebuilt intent. **Force-push method (simulation defect 3):** the API has no force-push listing — detect via (a) compare API against the known base and (b) author/committer-date skew on head commits (committer date = rewrite time); **label every skew result INFERENCE** until confirmed by a commit list, never report it as fact.
2. **Every commit:** every diff line vs live code, tests, reviewer comments; flag branding, carried drift, lost fixes.
3. **Every file:** line by line; docs must match code.
4. **Every review thread:** each note → the commit addressing it or the gap; where the reviewer's last word has no answer, draft a reply naming the new head SHA; post it once the audit completes (**Tier A** — all five audited PRs are the owner's own threads). **Probe endpoints + empty-meaning (simulation defect 4):** `pulls/{n}` (state/head/mergeable) · `commits/{head}/check-runs` (CI) · `pulls/{n}/reviews` (FORMAL approve/request-changes only) · `issues/{n}/comments` (thread discussion). An empty reviews response means **no formal reviews exist** — reviewer discussion lives in the issue-comments surface; distinguish that from a probe failure (which reports `probe-failed`, never "no reviews").
5. **Head + CI:** exact head SHA on GitHub + CI status on that head (match job ids OR check-run names — see §0 CI note).
6. **Resolve table + output contract (simulation defect 2):** per PR — head SHA · CI · force-push list (with INFERENCE labels) · open threads · disposition. **Probe JSON persists at `<run-dir>/Q3-audit.json`; the human table goes in the final report.** Zero writes, zero pushes.

## 11. README INSTALLATION DRAFT (post-decision docs; claims bounded to what ships)

> ### Install the agent skill
> `scripts/install-skill.sh` probes which agent harnesses are installed on this machine and deploys `skills/ix` to each one — Claude Code's `~/.claude/skills`, Agents' `~/.agents/skills`, Codex's `~/.codex/skills`, Gemini's `~/.gemini/skills`, and more. A harness is listed only when its skills directory exists and carries the expected shape — nothing is detected from an unrelated folder's presence. `--dry-run` previews the targets; pass harness ids to install only those.
>
> ### Register the MCP server
> `ix mcp install` registers the Ix MCP tools with your harness. It never overwrites a registration it did not write (`--force` replaces, `--host` narrows), keeps a `.bak`, and refuses a config file that does not parse. `--dry-run` writes nothing.
>
> ### Verify
> `ix doctor` reports install and backend state; `ix --version` prints to stdout.

Variant rules: **PORT outcome** → append: "Discovery of harness CLIs installed outside PATH is built in — a bounded, truncated-truthful scan of the common install roots." **KEEP / SPLIT outcome** → ships unchanged; no `Alot1z/*` repo links and no personal names — but the tool-contract docs the kept seam requires (TOOLSCAN_PATH usage etc.) stay intact (audit F7 batch: do not strip functional docs).

## 12. TUI LOGO — state + contract (own-repo surface; never rides #591)

- Canonical renderer: `repo-fork/scripts/render-logo.mjs` @ `d2876f5` — verified against the fork commit this session (byte-identity suite green). CLI surface, verbatim from the module (audit F4: pin the real one; the zip's `true|256|ascii` / `--width 8..400` surface is superseded, not canonical): `node scripts/render-logo.mjs [--width N] [--color auto|truecolor|256|ascii] [--file path] [--json]` · `--width` integer 8..120 (rows = clamp(round(cols·h/w/2), 2, 60)) · exit 0 ok · 1 usage/file · 2 unsupported/truncated · `auto` = NO_COLOR or TERM=dumb or FORCE_COLOR=0 → ascii; COLORTERM contains truecolor or FORCE_COLOR=3 → truecolor; TERM set → 256; else ascii · errors → stderr (JSON on `--json`), payload → stdout only.
- Test discipline — ALL 10 pins in `ix-cli/src/cli/__tests__/bootstrap-notice.test.ts` (the pin list IS the contract; weakening any of them is a regression): (1) emitSetupNotice writes stderr, never stdout; (2) banner shown when renderer+asset present (stderr contains renderBanner() output, not the text fallback); (3) absent-safe: renderer missing → plain-text heading, never throws; (4) renderBanner non-empty, contains ▀ half-blocks, non-empty lines indented; (5) NO_COLOR render has ZERO escape sequences (mutation-checked red→green) and never stdout; (6) `--json` honesty block: ok/tool/truncated false/0 < cells.ink < cells.total; (7) usage error: exit 1, reason on stderr, stdout payload-free; (8) renderLogo() library ≡ CLI byte-for-byte; (9) resolveColorMode env matrix; (10) LogoError carries toolscan-aligned exit codes. Full suite green before any push.
- Banner wiring: `ix-cli/src/cli/banner.ts` spawns the renderer (`--width 48`), stderr-only via `emitSetupNotice`, absent-safe (renderer/asset missing → plain-text heading, never an error). The PNG (`assets/logo.png`, sha256-identical to the owner's attached file) is the only art ever maintained.
- Test discipline to preserve: 10 pins in `bootstrap-notice.test.ts` incl. NO_COLOR zero-escapes (mutation-checked) + lib≡CLI byte-identity + JSON honesty block. Full suite green before any push.
- Backlog: `--bg none` transparent mode (from the superseded zip) — adopt with the same discipline. Fork PR → upstream main, its own clean story, AFTER the #591 decision lands.

## FINAL RULE

Spec = starting point, not literal command. AUDIT → IMPROVE → REPLAN → BUILD → VERIFY → REVIEW → GATE → RECORD → CONTINUE. Close the loop on every item; report per §9; the maintainer's reply on #591 is the only external dependency of this run — everything else is executable now.
