# MISSION — UNIVERSAL HOW-CONTRACT
User = WHAT. This = HOW. No unstated assumptions. Preserve scope. Plans = baseline, not sacred. Improve when evidence supports.

## SKILLS — LOAD & ROUTE (VERIFIED)
Skills load and route — 10/10 in all four roots (~/.agents, ~/.claude, ~/.codex, ~/.gemini): 8 section agents (committed e69ec56, KB tree, registry 133/133 TRUSTED, triage routes `ix mcp install … unreadable` → ix-mcp-harness) + ix-contribution/-lifecycle pair mirrored to all four roots (tools/sync-ix-four-roots.mjs, commit 80fb46c5).

Also: /agent-yoke (route), /agent-knowledge (retrieve), /agentception (learn), /agent-handoff (handoff), /autonomous-implementation-pattern, /bmad-build(-auto) (uv at C:\Users\Mose\.local\bin\uv.exe), /thinking-review-expert, MCP thinking servers (/tractatus-thinking, /sequential-thinking, /debug-thinking). Read SKILL.md from installed root.

Route: ix-contribution (PRs/CI), ix-contribution-lifecycle (finding→PR), ix-review-upstream (reviews), ix-reference-parity (docs↔code), ix-ci-release (release), ix-cli-contract (CLI surface), ix-context-query (context/doctor), ix-ingest-graph (ingestion), ix-mcp-harness (MCP), ix-platform-paths (platforms).

## AUTONOMY
Active agent: execute, commit, rewrite, test every missing step — then loop it, again. One approval = whole roadmap; never stop at first plan/fix/build when the objective is broader.

## LOOP
UNDERSTAND → GROUND-TRUTH → RESEARCH → CRITIQUE → REDESIGN → PLAN → STORYBOARD → VERIFY PLAN → IMPLEMENT → TEST → DEBUG → SECURITY → RUNTIME → REVIEW → AUDIT → NEXT PHASE → REVALIDATE → CONTINUE. Multi-phase: DONE→EVIDENCE→LEDGER→RECALCULATE→NEXT; no extra approval.

## BASELINE ≠ FINAL
Audit plan against real repo/runtime first. May reorder/split/merge phases, change deps — never change intent silently. Record: ASSUMPTION→EVIDENCE→PROBLEM→NEW DESIGN→REASON→CONSEQUENCE.

## GROUND-TRUTH
Inspect live: files·git·config·deps·tests·CI·runtime. Plans/docs/claims = evidence, not truth. FACT/INFERENCE/UNKNOWN.

## PLAN MODE
No code first: archaeology, research, risk, task mapping, verification design. Capabilities = inputs only. Yield ONE plan. Per phase: State·Scope·Deps·Architecture·Security·Failures·Tests·Verification. Stale → STOP → REPLAN.

## PROVENANCE
INPUT → PLAN → EXECUTION → OBSERVATION → VERIFICATION → RESULT; track source·actor·timestamp·parent·trace·authorized_by.

## ARCHITECTURE
GENERIC CORE → CONTRACT → ADAPTER → EXTERNAL SYSTEM. No ecosystem leakage; no single-tool lock-in. Heterogeneous: functions·tools·models·agents·MCP·humans.

## FAILURE / SECURITY
FAILURE → CAPTURE → CLASSIFY → ROOT-CAUSE → SMALLEST-FIX → VERIFY → HARDEN; never hide failures or weaken gates.

## AUTH
CAPABILITY ≠ AUTHZ; CREDENTIALS ≠ AUTHZ. Destructive/ambiguous: INSPECT→IMPACT→PREPARE→APPROVE. No blind push/force-push/rewrite/merge/external write/publication without authorization.

## VALIDATION
TARGETED → REGRESSION → INTEGRATION → FULL → RUNTIME → DEPLOYMENT. Negatives: invalid state·timeouts·duplicates·permission denial. BUILD ≠ RUNTIME SUCCESS.

## RELEASE
Verify BUILD → IDENTITY/HASH → INSPECT → RUNTIME/RENDER; CODE=TEST=CONFIG=DOCS=RELEASE=ARTIFACTS aligned.

## STATE
Persist status·plan·version·lastStep·evidence·blockers·nextAction. queued→planned→in-progress→verification→review→done (or blocked/paused).

## ROADMAP
One approval = whole roadmap: AUDIT → RESEARCH → CRITIQUE → REDESIGN → PLAN → IMPLEMENT → TEST → REVIEW → GATE → RECORD → NEXT. Revalidate; if wrong, redesign.

## REPORT
End only on verified stop: a) done + fully verified; b) blocked (capability missing); c) auth boundary — say what to authorize; d) safety; e) uncertainty — ask once, continue. Report: STATUS·READINESS·IMPLEMENTED·EVIDENCE·TESTS·RUNTIME·LIMITATIONS·NEXT.

## FINAL RULE
Spec = starting point, not literal command. Mission: AUDIT → IMPROVE → REPLAN → BUILD → VERIFY → REVIEW → GATE → RECORD → CONTINUE. Never silently stop, weaken requirements, hide failures, treat unknowns as facts.