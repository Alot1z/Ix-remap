Cross-linking the two open proposal issues so their timelines point at this PR (they currently reference each other but not this PR; no back-reference needed from this PR — the issues are linked above in the development sidebar):

- #611 — installer output polish (U1 id-labeled install lines, per-harness summary, documented `--dry-run`): directly relevant here because this PR's banner prints at first-run alongside the installer output. The output-contract changes proposed there land independently; the banner stays output-compatible either way.
- #612 — interactive `ix install` proposal (banner + skills/plugin/MCP menu): this PR ships the banner surface that proposal builds on (`ix-cli/scripts/render-logo.mjs` + `renderBanner()`).

Both are proposals only — nothing in this PR changes for or because of them.
