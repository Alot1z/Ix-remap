## Status: DRAFT — part of a larger train

This pull request is intentionally a **draft** and one component of a coordinated set the author is landing together (terminal logo banner · installer polish from the merged-surface audit · toolscan output-contract hardening). It will be marked ready only when every part of the train is green and the whole set is released at once.

Feedback and suggestions are welcome at any stage. Each point is addressed on this branch — folded in or answered — and merged without conflict. The content below is accurate; the head may still move.

---

## Rendered preview

![Ix logo banner as printed at first run — truecolor, brand backdrop](https://raw.githubusercontent.com/Alot1z/Ix-remap/meta/logo-previews/output-samples/banner-48-truecolor.png)

Width 48 is the CLI default (`--width 48`); a wider render ships alongside it at `output-samples/banner-80-truecolor.png` (same branch). Preview PNGs live on the `meta/logo-previews` branch so the PR tree carries only what ships.

## What this ships

The one-time setup notice (`ix` first run in a workspace) prints the Ix logo as a terminal banner instead of the plain text heading.

- `ix-cli/scripts/render-logo.mjs` renders `ix-cli/assets/logo.png` at print time. Zero dependencies: PNG chunk decode → inflate → unfilter → coverage-thresholded supersampling → flat 5-tone snap → half-block output. The PNG is the only maintained art; the terminal render is derived from it on every run. Both inputs ship **inside the package** (npm pack + release tarball), with a `npm pack` pin in the tests and a staging gate in `release.yml` so a future packaging change cannot silently strand them again.
- Truecolor with ANSI-256 and ASCII fallbacks. `--color auto` honors `NO_COLOR`, `TERM=dumb`, `FORCE_COLOR`; the no-color path emits zero escape bytes.
- `--bg none` paints only the logo's own pixels (unbacked `▀`/`▄` half-blocks), so the mark reads cleanly on light terminal themes. `--bg brand` (default) keeps the current backdrop. `--json` reports `bg` honestly.
- Absent-safe: a layout without the renderer or the asset falls back to the plain text heading — the banner is decoration, never load-bearing. stderr-only, so `--format json|llm` output stays clean.
- Golden fixtures in `output-samples/` (truecolor/256/ascii × brand/none) with output-stability pins.
- `assets/logo.png` already exists on main — no binary asset is added, only the renderer, wiring, tests, and goldens.
- Rebased onto main at the last branch update (`39d0734`); main's workspace-canonicalization change in `bootstrap.ts` is preserved (verified on a fresh clone of main). Upstream main has since advanced to `e8ab1926` (the #547/#559 exit-code wave merged, 2026-09-05); a final rebase onto that head runs before this PR is marked ready.

## Tests

- Focused suite: 21 pins in `bootstrap-notice.test.ts` — stderr-not-stdout, absent-safe renderer/asset, NO_COLOR zero-escapes, exit-code contracts, `--bg none` invariants (backdrop constant never appears as a background; library ≡ CLI byte-for-byte; JSON honesty), and byte-identity against the goldens.
- Mutation-checked locally: breaking the no-color contract turns the NO_COLOR pin red; disabling the `--bg none` emission path turns its pin red. Both restored green.
- Full `ix-cli` suite on the merged head (Windows): 95 files, 1704 passed / 21 skipped; `tsc --noEmit` clean.

## CI state (updated after the hermeticity fix)

Head `17d2da44` (fix round for the review findings): **all checks green except the single documented CodeQL infra-race run** (`101317167251`, started pre-recreation) — the six originally failing jobs (`Test` × ubuntu/macos × node 22/24, `CI Passed`) were fixed by the hermeticity change below and have passed on the head since the recreation re-ran the suite. The fix made the banner pins hermetic across runners — the half-block pin now pins its env (runners can resolve the ascii fallback otherwise), and the two lib≡CLI byte-identity pins got a realistic budget for the in-process full-PNG decode under v8 coverage (measured ~0.7 s → ~6.8 s with coverage instrumentation; assertions unchanged).

One residual, not a finding: the same documented **CodeQL** infra race (run `101317167251`, "The file may have changed since it was checked" — it started before the recreation, and the branch head moved during analysis). It needs a maintainer-side re-run (admin rights; the author's token is contributor-only) or clears on a branch update. **Update 2026-09-05:** the rendered preview PNGs moved the head to `c05c3a77`, so the suite re-runs on that head — including CodeQL.

## Notes

- The aesthetic approach (rendering a repo asset as a first-run terminal banner) takes some inspiration from skills.sh (vercel-labs/skills); the implementation here is original zero-dependency code. Per the author's convention, no attribution appears in any committed file.
- Part of a coordinated train with the installer README-claims fix (#609); this PR will be marked ready together with it when every part is green.
