{
  "probed_at": "2026-09-05T16:38:05.049Z",
  "digest": "searched reviewed-by:josephismikhail in ix-infrastructure/Ix → 66 total, sampling 8\n\n== PR #593 \"feat(ingest): parse CUDA (.cu/.cuh) and Python stubs (.pyi)\" | closed | comments-by-josephismikhail: 7 | reviews-by-josephismikhail: 7 ==\n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [inline @languages.ts:45] @C:/Users/josep/AppData/Local/Temp/rev593/c1.md\n  [inline @languages.ts:55] @C:/Users/josep/AppData/Local/Temp/rev593/c2.md\n  [inline @index.ts:2182] @C:/Users/josep/AppData/Local/Temp/rev593/c3.md\n  [inline @index.ts:2018] @C:/Users/josep/AppData/Local/Temp/rev593/c4.md\n  [inline @languages.ts:56] @C:/Users/josep/AppData/Local/Temp/rev593/c5.md\n  [inline @queries.cuda.test.ts:108] @C:/Users/josep/AppData/Local/Temp/rev593/c6.md\n  [inline @extension-parity.test.ts:19] @C:/Users/josep/AppData/Local/Temp/rev593/c7.md\n\n== PR #570 \"fix(map): stop blaming the language when the ingest was deduplicated\" | closed | comments-by-josephismikhail: 4 | reviews-by-josephismikhail: 4 ==\n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [inline @map.ts:181] `idempotentPatches >= patches` only proves that every patch *this run submitted* was a replay — it says nothing about the files the run never submitted, and those are the ones that disprove the new diagnosis.\n\nConcrete case: a workspace on a language the backend cannot build a hierarchy for (the exact case the old word…\n  [inline @ingest.ts:2505] `Idempotent` is not the only status that is counted as applied while writing nothing. Both backend handlers return `BaseRevMismatch` and write nothing when the optimistic base-rev check loses a race (`BulkPatchCommitRoutes` reads `queryApi.getLatestRev` outside the transaction and `commitPlan` compares it against `load…\n  [inline @ingest.ts:920] The const was inserted between `IngestFilesSummary` and its doc comment, so both hovers are now wrong: `/** Minimal local-ingest facts needed by commands that continue after ingestion. */` documents `COMMIT_STATUS_IDEMPOTENT`, and the wire-value comment documents the interface. Move the new const above that line (or be…\n  [inline @ingest.ts:823] \"`--full` re-submits every file\" does not describe any path in this tree. `ix map --full` only forwards `full` to `client.map(...)`; the `ingestFiles(cwd, ...)` call above it never passes `force`, so `--full` cannot re-submit anything. The flag that re-submits is `ix ingest --force` (`opts.force` clears the mtime cache…\n\n== PR #592 \"fix(test): stop the ignore assertion blaming .gitignore when git will not answer\" | closed | comments-by-josephismikhail: 3 | reviews-by-josephismikhail: 3 ==\n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [inline @watch-dedup.test.ts:234] **The skip fires exactly where the probe is most valuable -- and the refusal is avoidable here.**\n\n`fs.existsSync` follows symlinks. In the very layout this PR targets (a worktree borrowing artifacts from a sibling clone), `ix-cli/dist` is a symlink to a real directory, so `existsSync(probe)` is true, `target` becomes …\n  [inline @watch-dedup.test.ts:202] **This fallback is a tautology, and a checkable claim was available instead.**\n\n`cacheRoot` is `path.join(packageRoot, \"node_modules\", \".cache\")` by construction (`childBuildCacheRoot`), so `path.relative(packageRoot, cacheRoot).split(path.sep)[0]` is the literal string `\"node_modules\"` no matter what `.gitignore` says…\n  [inline @watch-dedup.test.ts:194] **The detector matches an English string that git translates.**\n\n`\"pathspec '%s' is beyond a symbolic link\"` is a `die(_(...))` in `pathspec.c` -- it goes through gettext. A contributor whose shell sets `LANG`/`LC_ALL` to a locale git ships a translation for (de, fr, zh_CN, ... all in `po/`) gets the localized text on …\n\n== PR #578 \"docs: a complete per-command flag reference, generated from the CLI itself\" | closed | comments-by-josephismikhail: 6 | reviews-by-josephismikhail: 6 ==\n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [inline @dump-cli-surface.mjs:70] **`--flags` cannot serve as the parity check it is advertised as.** It de-duplicates long names across the whole tree, collapsing the 196 (command, flag) pairs this script walks into 76 names. Per-command drift is therefore invisible: add `--limit <n>` to `ix stats`, or drop `--path` from `ix contains`, and this output…\n  [inline @dump-cli-surface.mjs:58] **Negated options dump the wrong default, and the table inverts because of it.** Commander leaves `defaultValue` `undefined` on a `--no-x` option and supplies `true` at parse time instead (verified: a command with only `.option(\"--no-recursive\", ...)` yields `{ recursive: true }` when the flag is absent). So `--no-recu…\n  [inline @dump-cli-surface.mjs:65] **The root program's own options are never walked.** Starting at `program.commands` means anything declared on `program` itself is dropped. The shipped CLI declares `.version(cliVersion)` on the root in `src/cli/main.ts:64`, so `ix --version` / `-V` is a real flag that appears in neither the JSON nor `--flags`, while f…\n  [inline @dump-cli-surface.mjs:22] `createRequire` is imported and never used — the module is loaded with dynamic `import()` on line 31 instead. Nothing catches it: `npm run lint` is `eslint src`, and knip's `project` is `src/**/*.ts`, so a script under `ix-cli/scripts/` is outside both. Drop the line.\n\n```suggestion\n```\n  [inline @flags.md:263] **This claim is false on the surface the file scopes itself to.** \"...forward to `goal` and `bug`, whose help documents the subcommand that absorbed them\" only holds with @ix/pro installed. On OSS, `goal`/`bug` exist solely as the `registerProStubs` placeholders (`.argument(\"[args...]\")`, no subcommands), so `workflows…\n  [inline @flags.md:45] **Two things off in this bullet, and the omission is the one that bites.** (1) `--limit` does not drive auto-pagination: `loadSubsystemScores` pages whenever `--detailed` is set and `explicitPage` is false, i.e. neither `--offset` **nor `--regions`** was passed (`subsystems.ts:527`); `--limit` only sets page size, and …\n\n== PR #577 \"docs: document ix savings, its flags, and its llm records\" | closed | comments-by-josephismikhail: 5 | reviews-by-josephismikhail: 5 ==\n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [inline @llm-format.md:179] `money_saved=202.71` is not a value this command can print. `renderSavingsLlm` renders `estimateMoney(tokensSaved, model).toFixed(2)`, and for the `tokens_saved=6142880` on this same line that is `(6142880*0.7*15 + 6142880*0.3*75)/1e6 = 202.71504`, which `toFixed(2)` gives as `202.72`. The session row above checks out …\n  [inline @llm-format.md:179] The `--detail` sample shows only `scope=session` command records, but `renderSavingsLlm` iterates `[[\"session\", ...], [\"lifetime\", ...]]` and, when `detail` is set, emits the `byCommandType` rows for **both** scopes -- so real `ix savings --detail --format llm` output on this data (lifetime `commands=4820`) also contai…\n  [inline @commands.md:77] `session` (this shell's commands) is a mechanism claim the CLI cannot support. `IxClient.savings()` issues `GET /v1/savings` (plus `?detail=true`) with no session id, no header, and no pid -- `savingsReset` likewise -- so the backend has nothing to attribute a count to a shell with; whatever `session` means, it is a wi…\n  [inline @commands.md:86] Wrong relative path: this file already lives in `skills/ix/references/`, so `references/output-formats.md` resolves to `skills/ix/references/references/output-formats.md`, which does not exist. The `references/...` form used in `SKILL.md:62,77` is correct only because `SKILL.md` sits one level up. An agent told to load…\n  [inline @commands.md:88] The bolded **lifetime** asserts more than anything in the repo establishes. The only contract for this call is `docs/api/README.md:448` -- \"DELETE `/v1/savings` -- Reset savings metrics\" -- with no scope, and `savingsReset()` sends a bare DELETE with no body or query, so nothing on this side of the wire selects a scope…\n\n== PR #582 \"docs: fix the dead backend repo link, and say what external contributors do\" | closed | comments-by-josephismikhail: 4 | reviews-by-josephismikhail: 4 ==\n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [review COMMENTED] \n  [inline @PULL_REQUEST_TEMPLATE.md:27] The relative link is correct only when this file is read in the repo tree — but this file's job is to be pasted into a PR body, where the base URL is the PR page, not `.github/`. GitHub does not rewrite relative hrefs in issue/PR bodies: `gh api markdown -f mode=gfm -f context=ix-infrastructure/Ix` renders `[BD](../CON…\n  [inline @CONTRIBUTING.md:157] This sentence asserts that `ix-memory-layer` is used correctly as an *artifact* name in the release workflow, but the workflow still uses it as a repository name. `.github/workflows/release.yml:19-21`:\n\n```\n# Note: The Scala backend (memory-layer) is built and released separately\n# from the private ix-memory-layer repo…\n  [inline @CONTRIBUTING.md:165] The subject of \"it installs\" here is `./scripts/backend.sh up`, but `docs/prerequisites.md` documents the `curl -fsSL https://ix-infra.com/install.sh | sh` installer, not that script — its content is Homebrew, git, Node 22, npm, Docker, ripgrep, the pre-built CLI tarball, `~/.ix/*` directory creation and the installer'…\n  [inline @CONTRIBUTING.md:177] Scope note, not a code defect: the PR body says `Closes #574`, but #574 names three gaps — the CONTRIBUTING link, the PR-template line, and `SECURITY.md` (\"has its own reporting channel\" with no channel named). The PR deliberately leaves the third one, and explains why. That reasoning is right, but `Closes` will auto-c…\n\n== PR #466 \"fix(upgrade): stop telling people to upgrade a backend that is current\" | closed | comments-by-josephismikhail: 0 | reviews-by-josephismikhail: 1 ==\n  [review APPROVED] Reviewed across thirteen rounds, with the fixes pushed to this branch. The diagnosis in the original description was right; the mechanism was not, and replacing it took most of the work.\n\n**Why the original approach could not stand.** `chec…\n\n== PR #465 \"fix(ingest): bound the manifest reads a scanned repo controls\" | closed | comments-by-josephismikhail: 0 | reviews-by-josephismikhail: 1 ==\n  [review APPROVED] Reviewed across three rounds, with the fixes pushed to this branch. The approach was right; two real defects sat inside the guard itself.\n\n**The cap did not bind.** `fs.readFileSync(handle)` re-stats the handle and reads *that* size, not th…"
}