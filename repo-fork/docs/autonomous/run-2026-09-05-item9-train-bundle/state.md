# State — run-2026-09-05-item9-train-bundle

**Date:** 2026-09-05 (state written ~16:10Z; PR recreated 15:45Z)
**Scope:** train bundle + illustrated TUI-logo showcase + item 9 (prepared, awaiting owner go) + item 10 (drafted).
**Live state (2026-09-05, after the close-out round):** two train drafts from us, draft-from-open with **requested reviewers: NONE** — **#605** (logo banner, head `c05c3a77` = 0869a137 + the preview commit adding `output-samples/banner-48/80-truecolor.png`; body now includes a Rendered-preview picture) and **#609** (installer README-fix, head `b77c4f52`, branch `feat/installer-readme-fix` off `e8ab1926`). Bodies cross-linked. **#610 + #608 CLOSED** (owner's call — superseded-by-process, notes posted, branch deleted). PR **#604** — CLOSED (recreated as #605). Toolscan main `4c0b2d11`. Issues: #608 + #606 both closed. The repo's only other open PR is #607 (josephismikhail's own, not ours). Nothing marked ready — mark-ready (#605 + #609) stays gated on the owner's go at item 9.

## Why #604 closed and #605 opened (owner correction, 2026-09-05)
#604 was opened as *ready* and later converted to draft. GitHub auto-requested the `* @josephismikhail` code-owner review 1 s after that ready-open — and removing a pending review request once a PR is a draft is blocked by a GitHub-side bug (community #69208, open since 2023; reproduced 3/3 DELETEs → 422, re-add → 404). GitHub docs: code-owner requests fire at **mark-ready**, never at draft-open. So #604 was closed and **recreated as #605 draft-from-open** on the same branch/head/body — which carries zero requested reviewers, verified live. Universal rule recorded as agent-knowledge **#6646** (VERIFIED/TRUSTED): drafts get no review requests; never request reviewers on a draft; if a request is stuck, recreate draft-from-open.

## Artifacts
- `pr-605-illustrated-showcase.html` — **the full TUI-logo showcase** (preview222-style): 15 pixel-art variants (truecolor/256/ascii × 48/80/112 × brand/none) rendered **live from the PR-head renderer**, raw-ANSI textareas, the art, live #605 state. Replaces the icon-copy report (`pr-604-illustrated-report.html` kept as the earlier record).
- `.logo-pr-body-v2.md` — #605's body: DRAFT + train framing, **welcome-feedback** wording (comments/suggestions appreciated; issues addressed on-branch, merged without conflict).
- `TRAIN-REPORT.md` / `ITEM-9-BUNDLE.md` / `ITEM-10-FINAL-REPORT.md` — earlier-session record; item 9 NOT executed (owner re-confirmed drafts-only), item 10 drafted.
- `pr-data.json`, `pr2-data.json`, `pr-recreated.json`, `logo.png`, preview outputs — probe payloads + art.
- Scratch (`.`-prefixed): `.probe2.mjs`, `.fix-reviewer*.mjs`, `.diag-reviewers.mjs`, `.recreate-pr.mjs`, `.post-close-comment.mjs`, `.check-probe.mjs`, `.build-showcase.mjs`, `.logo-pr-title.txt`, `.logo-pr-body-v2.md`, `.close-comment.md`.

## Verification (this session)
- #604 closed (API state=closed, closed_at 15:45:07Z); closing comment posted verbatim, by Alot1z.
- #605: draft=true, head `0869a13761`, requested_reviewers `[]`, body byte-equal to `.logo-pr-body-v2.md` (all live-verified).
- Showcase: 15 variants rendered from `render-logo.mjs` at the PR head; DOM-verified (7 sections, 14 pixel grids, 489-tone hero).
- KB #6646 added → verify-gate PASS → promoted TRUSTED (VERIFIED).
- Local fork record commit `37741ff` (earlier-phase dirs, 15 files, NO push).
- `dispatch-check.mjs`: fast + `--live` **ALL GREEN** (live pins now #604 closed + #605 draft@0869a137 + zero reviewers).

## Session update (2026-09-05, drafts-only round — zero live changes)
- **`draft-body-comparison.html`** — installer kit body vs the **live** #605 body, side by side as reviewers see them; verdict: one voice (identical status header + verbatim feedback line; one intentional per-PR accuracy-line variance).
- **`live-drafts-snapshot.md`** — one-screen live probe: #605 draft @ `0869a13` · 0 reviewers · CI 29/1 (CodeQL race) · fork `Alot1z/Ix-remap` 0 PRs · toolscan 0 PRs (direct-commit convention) · issues #608 open / #606 closed. The only other open PR anywhere: #607 (owner's own, not ours).
- **Draft-body standard checklist** added to dispatch §1 item 9 + the installer `OPENING-KIT.md` + ITEM-9-BUNDLE §2 (header · verbatim feedback line · accuracy line · scope · no do-not-review framing · post-open reviewer verify) — dated 2026-09-05.
- **#608 fix kit grounded:** CONTRIBUTING.md Development Workflow step 4 is the single honest home (one added line, docs-only; PR template has no review-flow section → no change there). Kit title/body updated to the concrete change, Refs #608, held for train release.
- KB **#6653** (no orphan issues — issues open only alongside their PR; close-with-note is the hold) added, gate PASS, promoted TRUSTED. Full historical thread sweep: zero new rows (campaign fully mined).
- Workspace renamed `ix-workspace` → `github-workspace` (plain descriptive name; commits `b22f7c9`+`82b2bdf`, no remote).
- **E1–E8 compliance pass (2026-09-05, `e1-e8-kit-compliance.md`):** upstream main advanced to `e8ab1926` — the #547/#559 exit-code wave merged (awaiting-external NONE). All four upstream-facing bodies re-grounded at the new head: installer + C3 kits PASS unchanged; #608 fix kit re-grounded (`39d0734`→`e8ab1926`, CONTRIBUTING step 4 verified unchanged); **#605 live body fixed for genuine E1 violations** — stale CI count and the now-false "current main (`39d0734`)" claim — via the gated verbatim edit (draft kept, reviewers zero, live-verified). #605 now carries a rebase-onto-`e8ab1926` before mark-ready (recorded in ITEM-9-BUNDLE §2 step 3).
- **Wave closed out + rebase preflighted (2026-09-05):** all three plugin gates merged (15:35–15:44Z), then #547 → `df30296` (15:47Z) and #559 → `e8ab1926` (15:57Z, main head); ARMED #547 rebase resolved as MOOT — no record reads "no movement" (item-7 run-dir `state.md`/`wave-state.md` updated). #605 rebase dry-run in the scratch verify tree: **clean, zero conflicts, byte-identical tree, zero file overlap** — exact release-time steps at `item9-rebase-runbook.md` (referenced from ITEM-9-BUNDLE §2 step 3).

## Session update (2026-09-05 — draft-opens round, owner's go)
- **#609 + #610 opened live as DRAFTS** (Tier A.3): fork branches `feat/installer-readme-fix` (`b77c4f52`) and `docs/contributing-draft-first` (`4caaf508`) created at upstream main `e8ab1926`; contents built from the live `e8ab1926` text — README.md (3 corrections, exact-match applied, byte-minimal diff) and CONTRIBUTING.md (one line added to Development Workflow step 4) — snapshot + lease push via `gh-commit.mjs`, opened via the gated `gh-api.mjs` path with `draft: true` at create (**gh-pr.mjs has no draft flag — recorded as a KB row**), each verified post-open: draft=true, requested_reviewers=[], body byte-verbatim. CI in progress on both (docs-only).
- **Cross-links live:** #605 body now names #609/#610; #609 names #605/#610; #610 carries Refs #608 + names #605/#609. No new issue opened — a docs-only README fix warrants none (KB #6653; #608 is the sole related issue, already open). C3 stays held per its own kit gate.
- **Checker hardened:** `dispatch-check.mjs --live` now pins #609/#610 (must be draft with zero requested reviewers) alongside #605.
- Triage for the round: ix-platform-paths + ix-review-upstream (ix-agent-triage). Scratch files (`.`-prefixed) cleaned or kept as probes in this run-dir; corrected-file build/verification scratch at `.scratch/`.
- **Close-out round (2026-09-05, owner's call):** **#610 + #608 closed as superseded-by-process** — they document GitHub's ready-open code-owner behavior but change none (no repo setting/CODEOWNERS edit can disable it; removal-block = platform #69208), and the real prevention is structural (KB #6646/#6658 + dispatch-check pins). Notes posted, branch `docs/contributing-draft-first` deleted. **#605 gained the TUI logo picture** (owner ask): PNGs committed at `output-samples/banner-48/80-truecolor.png` (head `c05c3a77`), Rendered-preview section added to the PR body (live PATCH, verbatim file kept), and `tui-banner-picture.html` built in this run-dir (registered in the Preview tab). **doctor.mjs** gained the 6f `--draft`-surface pin and the 6c2 repo==deploy mirror check — ALL GREEN. **agent-principles is now version-controlled**: canonical source at `agent-knowledge-base/skills/agent-principles`, deploy via `scripts/sync-agent-principles.sh`, committed `460a3dc` (no push, `.gitattributes` `eol=lf`).
- **KB #6658 implemented (2026-09-05, tool upgrade):** `gh-pr.mjs create` gained `--draft` (draft:true at create; ready-open default unchanged + asserted), so draft-from-open is now the standard tool surface — no more hand-rolled gh-api.mjs drivers for draft opens. `e2e-scratch.mjs` step 4b pins it live (ready default NOT draft; `--draft` → draft:true, verbatim title/body, zero requested reviewers, open→close): **ALL PASS (28 API calls)**. `doctor.mjs` **ALL GREEN**. Live change at `C:/Users/Mose/.agents/skills/agent-principles/tools/` (no enclosing git repo → no commit); record copy + diffs at `tool-upgrade-kb6658/` in this run-dir; backups at `.backup-tools/`. KB #6658 updated to implemented.
- **CI note (#610, 2026-09-05):** on a CONTRIBUTING.md-only diff, the suite's one red check is the **pre-existing Windows `parse-pool.test.ts:137` timing assertion** (run `101343377885`, `windows-2022 · node 22`) — the same timing-sensitive area #607 exists to fix; unrelated to this docs change, not re-run. #609 is fully green (24 success). #605 unchanged (50 success + the documented CodeQL race).

## Session update (2026-09-05 — install demo + GIF round)
- **Local install demo package** at `install-demo/` in this run-dir: `run-install-demo.sh` runs the real `scripts/install-skill.sh` fully hermetic (`HOME`/`USERPROFILE` → `tmp-home/`), captures `transcript.txt`, and rebuilds `install-demo.gif` (75 frames, 872×550, PIL) — the whole install watchable end to end: dry-run JSON → per-host detection (claude/agents/codex/cursor, `detectedVia` path vs config-dir) → real copy → final tree. README documents re-run + the public-install paths.
- **⚠️ Disclosure (mid-round, corrected):** one early non-hermetic demo run wrote the real harness dirs (the installer's designed action; byte-identical `skills/ix` copies). Rolled back same round — copies preserved at `install-demo/rollback-realhome/`, all four real homes verified clean. The runner now clears `HOME`/`USERPROFILE` into `tmp-home` up front; script prints `HARNESS_HOME=` on line one.
- **Public-install answer grounded (2026-09-05):** the demo exercises the repo's own bootstrap (`bash scripts/install-skill.sh`, works today). Zero-clone user paths are all upstream-owned: standalone `curl https://ix-infra.com/install.sh | sh`, Homebrew `Formula/ix.rb` + release tarball via `scripts/release.sh`, and the npm path (`@ix/cli` v0.10.5) which **needs the team's npm publish** before `npx @ix/cli …` works. Table recorded in `install-demo/README.md`.

## Session update (2026-09-05 — viewer + UX kit + runbook pass-3 round)
- **`install-demo/index.html`** — self-contained viewer (GIF inlined as data-URI + transcript embedded), registered in the Preview tab; verified rendering (GIF 872×550 complete, transcript 3,176 chars). Note: the preview server serves only the registered file, hence the inline build.
- **`installer-ux-polish-kit.md`** (item-2 run-dir) — concrete UX improvements drafted from the demo transcript: U1 per-host id labels on install lines (with the exact two-line CI grep updates needed — verified against `ci.yml:374/479`), U2 per-host summary table from the existing `DECISIONS` array, U3 real `--help`, U4 dry-run footer naming the `--json` variant; explicit not-proposed list (`--prefix` flag, spinners, color — with reasons). Held local, nothing opens.
- **Runbook pass 3 (real rebase executed):** fresh clone + fetch of the upstream URL (a fork-clone `origin main` fetch does NOT bring `e8ab1926` — pass 3 proved it), actual `rebase --onto` run: **4/4 clean, head tree `95b0ad37…` matches the merge-tree prediction exactly, PR diff after rebase = 13 banner files only.** Runbook updated with the pass-3 identity check (step 3 pins the tree sha) and the fetch-URL gotcha (step 1).
- **Release note refreshed** for the duo reality (#610/#608 closed) + picture head; posting rules updated (the named set = #605 + #609; toolscan named as context only).
- `scripts/install-skill.sh` unchanged on main since `39d0734` (verified via `git diff 39d0734 origin/main` — empty), so the UX kit's line references hold on `e8ab1926`.

## Session update (2026-09-05 — KageBinary review round: #609 merged, #605 fix head)
- **Live events:** #609 **MERGED** (main → `8c0e6b00`; reviewer verified all four README corrections against the registry and merged standalone, explicitly decoupling it from #605). #605 reviewed at `c05c3a77`: **3 findings** (1 blocking: banner inputs at repo root = unreachable in both shipped layouts; 2: CodeQL `js/file-system-race` high on statSync/readFileSync TOCTOU + FIFO hang — the alert is real, not the infra race the body claimed; 3: two preview PNGs referenced nowhere) + design note (in-process import would remove the spawn).
- **Track A — fix head `17d2da44`** (branch `feat/tui-logo-banner`, LOCAL, 5 commits over `8c0e6b00`): F1 renderer+asset moved into `ix-cli/` (`ix-cli/scripts/render-logo.mjs` + `.d.mts`, `ix-cli/assets/logo.png`), `banner.ts` rewritten to in-process dynamic import behind a package-relative probe (probe-then-import keeps the absent-safe contract — a static import crashed the watch child runtime cache layout, caught by the suite, fixed); deliverability pinned BOTH sides: npm-pack assertion in `bootstrap-notice.test.ts` + `release.yml` staging copies with a hard `banner inputs missing` publish gate. F2 TOCTOU: `readPngFile()` opens once (`O_NONBLOCK`), `fstat` on the handle, `isFile()` + size on that stat, reads the same fd (FIFO cannot hang, race closed). F3 preview PNGs dropped from the branch (copies at `banner-48/80-truecolor.png` in this run-dir + branch `meta/logo-previews` at `c05c3a77` for raw.githubusercontent body URLs). **Verified on fix head: 1755 passed / 21 skipped (95 files), typecheck clean, compiled `dist/cli/banner.js` renders 25-line banner in the installed layout, npm pack carries all 3 inputs.** Awaits owner go to push.
- **Track D — `chore/installer-ux-polish` @ `44fbb54`** (LOCAL, 1 commit over `8c0e6b00`): U1 (`would install [id]:` / `Installed [id]:`) + U3 (`--help` with live-registry ids + `try --help` hint on unknown-option) + U4 (dry-run footer names `--json`); the two CI smoke greps updated to the new line shape (kit's compat requirement). U2 (summary table) deliberately NOT on the branch — moved to the Track C upstream proposal. Exercised live: dry-run labels, help exit 0, unknown-option exit 1, hermetic HOME install. Awaits owner go to push (no PR — per green-light terms).
- **Track C — `installer-ux-upstream-issue-draft.md`** (item-2 run-dir): upstream-friendly proposal (U1 labels + U2 summary table; problem/evidence/minimal fix/compat notes). HELD — opens only on owner go after the train releases.
- **Track E — attribution-guard sentence wiped:** `.ts4-verify/agents.md` reworded to a plain conventional-commits line (untracked scratch, no history existed); `github-workspace/drafts/repo-blueprint.md` reworded + committed locally (no remote). Repo-wide sweep: zero hits in any tracked file of Ix-remap/toolscan; the sentence never reached a public repo.
- **Track F — toolscan upgrade `feat/agent-skill-package` @ `c1a9a16`** (LOCAL in `toolscan/repo-upstream`, 5 files +337): `skills/toolscan` package (SKILL.md with the real surface + fail-closed exit-2 contract; `references/composition.md` naming the Ix `TOOLSCAN_PATH` seam as the live integration case; `examples/agent-preflight.sh`), `scripts/install-skill.sh` (Ix-pattern installer: 4 verified harness conventions, `[id]` labels, `--dry-run/--json/--force/--with-cli/--help`, refusal guard with preview/real exit-1 parity — the parity bug was caught by exercising it and fixed), HD `assets/logo-2048.png` (239 KB, repo's own renderer). Push after owner review.
- Suite proof for release day recorded in the runbook (1752/1752 on `95b0ad37` earlier; now superseded by 1755 on the fix head).

## Pending
- Owner go on item 9 (the only mark-ready moment): rebase #605 onto `e8ab1926`, then mark ready together. Until then nothing is marked ready; C3 and all other kits stay held-local; zero further upstream writes.

## Session update (2026-09-05 — review-response + toolscan round)
- **KageBinary reviewed both drafts (#5554048209/#5554049672). #609 MERGED standalone** (main `e8ab1926`→`8c0e6b00`) — reviewer verified all four README corrections against the registry. **#605 got 3 findings + 1 design note.**
- **Suite proof on rebased head `95b0ad37`** (pre-review finding, release-day gate): 1752/1752 + pins 21/21 + typecheck clean. Scratch gotcha recorded: core-ingestion needs its own `npm ci` in scratch (10 false failures without it).
- **Track A — #605 findings implemented locally** (scratch clone `.fix605/`, branch head `17d2da4`, NOT pushed): rebased 4/4 onto `8c0e6b00`; **fix#1 deliverability** — `scripts/render-logo.mjs` + `d.mts` + `assets/logo.png` moved INTO `ix-cli/`, `banner.ts` rewritten to import the renderer in-process (dynamic, behind a package-relative probe — a static import crashes the watch child-runtime layout which copies dist/ without scripts/+assets/; the probe restores absent-safe in every layout; found by running the real suite, fixed same round); npm-pack pin + release.yml staging copy + `banner inputs missing` publish gate; **fix#2 CodeQL TOCTOU** — one-handle read (O_NONBLOCK open → fstat handle → isFile + size → read from fd), closes the race AND the FIFO hang; **fix#3** — preview PNGs dropped from the PR branch (preserved at `meta/logo-previews` fork-branch + run-dir copies for the body pictures). **Verified: 1755 passed / 0 failed, typecheck clean, compiled dist/cli/banner.js renders (25 lines), npm pack carries all three inputs, YAML parses.**
- **Track D — installer UX polish implemented** (scratch clone `.ux-5DQjcl/`, branch `chore/installer-ux-polish` @ `44fbb54`, NOT pushed, no PR): U1 `[id]` labels + U3 `--help` (ids read live from the registry) + U4 `--json` footer hint, both CI greps updated to the new line shape. U2 summary table NOT in the branch — moved to Track C as the upstream-discussion piece. Verified live: `--help` exit 0, unknown-option hint exit 1, dry-run labels, hermetic run.
- **Track C — upstream issue draft held:** `run-2026-09-05-item2-installer-audit/installer-ux-upstream-issue-draft.md` (U1+U2 as problem/evidence/minimal-fix; opens only on owner go, after the train).
- **Track F — toolscan agent-skill package** (Alot1z/toolscan repo-upstream, branch `feat/agent-skill-package` @ `c1a9a16`, NOT pushed): `skills/toolscan/` (SKILL.md teaching the real verbs + exit-2=fail-closed contract; `references/composition.md` with the Ix `TOOLSCAN_PATH` live case as integration example 1; `examples/agent-preflight.sh`), `scripts/install-skill.sh` (four verified harness conventions, dry-run/--json/[id] labels/refusal guard/--help — preview/real exit-1 parity bug found by exercising it and fixed), HD `logo-2048.png` via the repo's own zero-dep renderer. Live-tested hermetic: install, skip lines, JSON report, refusal exit 1 both modes, --force overwrite.
- Attribution-guard sentence wiped from `.ts4-verify/agents.md` + `github-workspace/drafts/repo-blueprint.md` (repo-blueprint change committed in that repo); no other copy exists in any tracked file (verified by grep; git-log sweep found no committed instance).
- **Pending pushes (all await owner go):** Track A fix branch → force-with-lease update of #605 + PR body refresh; Track D branch → push (PR opens on owner go); toolscan branch → push to Alot1z/toolscan. Records commit for this round follows.
